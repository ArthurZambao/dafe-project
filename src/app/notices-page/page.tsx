import { NoticesPageData } from '@/modules/notices-page/components/content';

export const metadata = {
  title: 'Notícias',
};

export default function NoticesPage() {
  return <NoticesPageData />;
}
